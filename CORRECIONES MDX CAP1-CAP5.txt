CORRECIONES MDX CAP1-CAP5

# AGENT PROMPT — TFG 28-A: Content Improvement Batch 1 (Chapters 0–3)
**Project:** Final Degree Project — Iberian Blackout of April 28, 2025  
**Author:** Alfonso Monge Díaz-Ángel  
**Site:** Docusaurus / MDX at `tfg-antigravity-docs/docs/`  
**Batch scope:** Improvements 1–5 from the content audit (highest priority, lowest effort)

---

## YOUR ROLE AND CONSTRAINTS

You are a senior technical editor and MDX developer working on an Energy Engineering thesis about the 2025 Iberian blackout. You have read/write access to all project folders.

**Non-negotiable rules before touching any file:**
1. **Read the full current file** before editing it — use your file-reading tool on the exact path. Never edit from memory.
2. **Preserve all existing JSX components exactly** — do not modify props, rename components, or alter any `import` statement unless the specific improvement explicitly requires it.
3. **Preserve all existing figure captions** (lines starting with `_Figura`) verbatim — do not reword them.
4. **Preserve the frontmatter block** (the `---` YAML section at the top) of every file verbatim.
5. **Write in Spanish** for all new prose content added to MDX files (the document language is Spanish). Exception: `ForensicTable` `title` attributes follow the existing SCREAMING_SNAKE_CASE English convention already used in the project.
6. **Do not introduce new React component imports** unless the component already exists in `@site/src/components/`. If unsure, check the `src/components/` folder first.
7. **Match existing code style exactly**: indent 2 spaces, use `{/* */}` for JSX comments, use backtick strings in JSX attributes only where already used.
8. After each file edit, output a short diff summary listing lines changed/added/removed.

---

## IMPROVEMENT 1 — `intro.mdx`: Add fallback academic metadata below `<ExecutiveHook />`

### File to edit
`tfg-antigravity-docs/docs/intro.mdx`

### Current state (full file)
```mdx
---
slug: /
sidebar_class_name: "hidden"
title: "Inicio"
hide_title: true
hide_table_of_contents: true
pagination_next: null
pagination_prev: null
---

import ExecutiveHook from "@site/src/components/ExecutiveHook";

<ExecutiveHook />
```

### What to add
After the `<ExecutiveHook />` line, append the following block **exactly**. Do not change anything above it.

```mdx
<div style={{
  maxWidth: '720px',
  margin: '3rem auto 0 auto',
  padding: '1.5rem 2rem',
  borderTop: '1px solid rgba(255,255,255,0.08)',
  color: 'var(--ifm-color-emphasis-600)',
  fontSize: '0.85rem',
  lineHeight: '1.7',
  textAlign: 'center',
}}>

**Trabajo de Fin de Grado — Grado en Ingeniería de la Energía**

Alfonso Monge Díaz-Ángel · Universidad Pontificia Comillas ICAI · Junio 2026

_Análisis crítico y comparativo del colapso del sistema eléctrico ibérico del 28 de abril de 2025:
narrativas técnicas, regulatorias e institucionales._

</div>
```

### Why
The landing page delegates 100% of its content to a React component. If `<ExecutiveHook />` fails to render (network error, SSR issue, JS disabled), the page is blank. This fallback div provides author, institution, and thesis description as plain HTML, improves SEO indexing, and ensures screen-reader accessibility without touching any existing component logic.

### Verification
After editing, confirm the file ends with the new `</div>` block and that the frontmatter and `<ExecutiveHook />` are untouched.

---

## IMPROVEMENT 2 — `01-introduccion.mdx`: Add key-figures info callout at the top of the chapter body

### File to edit
`tfg-antigravity-docs/docs/01-introduccion.mdx`

### What to add
Locate the line that reads exactly:

```
El 28 de abril de 2025, el sistema síncrono ibérico sufrió un colapso de tensión
```

Insert the following `:::info` callout **immediately before** that opening paragraph (after `<GlitchTitle>Introducción</GlitchTitle>` and any blank line, but before the paragraph text):

```mdx
:::info Magnitudes del incidente
| Parámetro | Valor | Fuente |
| :--- | :--- | :--- |
| Generación destruida en la cascada | >15 GW en <30 s | Comité de Análisis del Gobierno, 2025 |
| Demanda sin suministro (España) | 25,2 GW | ESIOS / REE |
| Demanda sin suministro (Portugal) | ~5,8 GW | REN / ENTSO-E |
| Personas afectadas | ~57 millones | Comité de Análisis del Gobierno |
| Duración del apagón (reposición 99,95%) | 18,5 horas | REE / ENTSO-E |
| Pérdida económica estimada (VoLL) | 1.000–1.500 M€ | CEOE / ATA / estimación propia |
| Coste acumulado "Operación Reforzada" | >666 M€ (10 meses) | Redeia, abril 2026 |
:::
```

### Why
The introduction opens directly into narrative text without anchoring the reader to the scale of the event. A concise table of 7 key figures provides immediate quantitative context, serves as a navigation anchor for subsequent chapters, and follows the best practice of leading technical reports with a "snapshot" before the analytical framing. The table uses Docusaurus-native `:::info` admonition syntax which requires no new imports.

### Verification
Confirm the `:::info` block appears between `<GlitchTitle>Introducción</GlitchTitle>` and the first paragraph. Confirm no existing text, figure, or component was moved or deleted.

---

## IMPROVEMENT 3 — `02-contexto.mdx`: Move `<SwingEquationSimulator />` to a collapsed details block with a gallery link

### File to edit
`tfg-antigravity-docs/docs/02-contexto.mdx`

### What to change
This is a **two-part edit**.

#### Part A — Replace the inline simulator with a link + collapsed details

Find this exact block (it appears in the section about "La ecuación de swing"):

```mdx
<SwingEquationSimulator />

_Figura X. Simulador interactivo de la Ecuación de Oscilación. Modifique los parámetros de inercia y observe el impacto sobre la RoCoF del sistema. Elaboración propia._

(también disponible en la [Galería de Gráficas Interactivas](./galeria-graficas.mdx#swing))
```

Replace it with:

```mdx
:::tip Simulador interactivo — Ecuación de Oscilación
Explora cómo varía la RoCoF al modificar la constante de inercia *H* del sistema.
[**→ Abrir simulador en la Galería de Gráficas Interactivas**](./galeria-graficas.mdx#swing)

<details>
<summary style={{cursor:'pointer', fontSize:'0.9rem', color:'var(--ifm-color-emphasis-600)'}}>▶ Ver simulador embebido aquí</summary>

<div style={{marginTop:'1rem'}}>
<SwingEquationSimulator />
</div>

</details>
:::
```

#### Part B — Remove the `SwingEquationSimulator` import **only if** it is not used anywhere else in the file after the replacement above

After applying Part A, scan the entire file. If `<SwingEquationSimulator />` no longer appears outside the `<details>` block (i.e., no other inline usage), keep the import line as-is — the component is still referenced inside the details. **Do not remove the import.**

### Why
`<SwingEquationSimulator />` is the fourth heavy interactive component in Chapter 2 (alongside `EnergyTransitionStreamgraph`, `MixGeneracion`, and `CollapseSismograph`). The chapter is in the saturation zone (4 heavy components). Collapsing the simulator into a `<details>` element reduces the visual weight for first-time readers while preserving full access for those who want to interact with it. The :::tip admonition makes the gallery link prominent. This is a zero-content-loss change.

### Verification
- The :::tip block with the gallery link renders at the same position in the document.
- `<SwingEquationSimulator />` is still accessible inside the `<details>` element.
- The import line `import SwingEquationSimulator from "@site/src/components/SwingEquationSimulator";` remains at the top of the file.
- `CollapseSismograph`, `MixGeneracion`, and `EnergyTransitionStreamgraph` are completely untouched.

---

## IMPROVEMENT 4 — `03-analisis-incidente.mdx`: Add a static 32-second chronology table at the start of the chapter and move `<UFLSVisualizer />` to a collapsed details block

### File to edit
`tfg-antigravity-docs/docs/03-analisis-incidente.mdx`

This is the most saturated chapter (7 heavy interactive components). This improvement has **three parts**.

---

#### Part 4-A — Insert 32-second chronology table immediately after the opening paragraph

Find the exact opening paragraph that ends with:

```
...identificar con precisión los mecanismos físicos que ningún automatismo de protección convencional pudo detener.
```

After that paragraph (and before the `## Fase 0` heading), insert the following block:

```mdx
<div style={{overflowX:'auto', marginBottom:'2rem'}}>

| t (CEST) | Δt | Evento | Fase |
| :--- | :---: | :--- | :---: |
| 12:03:00 | — | Oscilación forzada 0,6 Hz (amplitud 70 mHz) en Badajoz. Amortiguamiento ~1%. | 0 |
| 12:08:00 | — | HVDC INELFE-1 cambia PMODE3 → PMODE1. Pierde capacidad de respuesta dinámica. | 0 |
| 12:19:00 | — | Modo inter-área 0,2 Hz Este-Centro-Oeste. Península oscila en contrafase con Europa. | 0 |
| 12:30:00 | — | 82% del mix instantáneo no síncrono. Inercia zonal sur: 1,3 s. Demanda: 25.184 MW. | 1 |
| **12:32:57** | **t = 0 s** | **Disparo raíz: transformador 400/220 kV en Granada. −355 MW activos, −165 MVAr reactiva.** | **2** |
| 12:33:00 | t = 3 s | Tap-Lag activo: secundario 220 kV alcanza ~244 kV (>1,10 p.u.), invisible para SCADA REE. | 2 |
| 12:33:16 | t = 19 s | Cascada IBR: Badajoz −730 MW. Inicio Fase 3. | 3 |
| 12:33:18 | t = 21 s | Segovia, Sevilla, Huelva. UFLS activa a 49,5 Hz → agrava sobretensión (paradoja). | 3 |
| 12:33:20 | t = 23 s | −15.000 MW totales en cascada. >25,2 GW demanda sin suministro. | 3 |
| 12:33:21 | t = 24 s | Protecciones OST abren enlaces AC transpirenaicos. Península aislada del sistema europeo. | 3 |
| **12:33:29.741** | **t = 32 s** | **Cero de tensión definitivo. Último grupo generador dispara.** | **3** |

</div>

> **Fuentes:** Comité de Análisis del Gobierno (2025), ENTSO-E Factual Report (2025), IIT-ICAI / Compass Lexecon (2025), NREL PMU analysis (2025). Los mecanismos físicos de cada fila se desarrollan en las secciones siguientes.
```

---

#### Part 4-B — Fix the broken Figure 18 self-reference

Find this exact inline paragraph:

```mdx
<p style={{fontSize:'0.8rem', color:'var(--ifm-color-emphasis-600)', fontStyle:'italic', marginTop:'0.5rem'}}>Figura 18. Evolución acoplada de tensión y frecuencia durante la Fase 3 — Ver Figura 11 arriba (Sismógrafo del Colapso).</p>
```

Replace it with:

```mdx
<p style={{fontSize:'0.8rem', color:'var(--ifm-color-emphasis-600)', fontStyle:'italic', marginTop:'0.5rem'}}>Figura 18. Evolución acoplada de tensión y frecuencia durante la Fase 3. Ver <a href="./02-contexto.mdx#eas-falsa-normalidad">Figura 11 — Sismógrafo del Colapso (Capítulo 2)</a> para la evolución dinámica completa.</p>
```

---

#### Part 4-C — Collapse `<UFLSVisualizer />` into a details block

Find this exact block:

```mdx
<UFLSVisualizer />

_Figura X. Retrato de fases del UFLS: evolución acoplada de frecuencia-tensión durante el deslastre. La trayectoria evidencia la paradoja: el deslastre por subfrecuencia agrava la sobretensión. Elaboración propia._
```

Replace it with:

```mdx
<details>
<summary style={{cursor:'pointer', userSelect:'none', padding:'0.75rem 1rem', background:'rgba(255,165,0,0.06)', border:'1px solid rgba(255,165,0,0.2)', borderRadius:'8px', fontSize:'0.9rem', color:'rgba(203,213,225,0.85)', listStyle:'none', display:'flex', alignItems:'center', gap:'0.5rem'}}>
  <span>▶ Simulador interactivo — Retrato de Fases del UFLS (paradoja de la subfrecuencia vs sobretensión)</span>
</summary>

<div style={{marginTop:'1rem'}}>
<UFLSVisualizer />

_Figura 18b. Retrato de fases del UFLS: evolución acoplada de frecuencia-tensión durante el deslastre. La trayectoria evidencia la paradoja: el deslastre por subfrecuencia agrava la sobretensión. Elaboración propia. También disponible en la [Galería de Gráficas Interactivas](./galeria-graficas.mdx#phaseplane)._
</div>

</details>
```

**Do not remove the import line** `import UFLSVisualizer from "@site/src/components/UFLSVisualizer/UFLSVisualizer";` — the component is still referenced inside the details.

---

### Why (Part 4 overall)
- **Part 4-A:** The 32-second table gives readers a cognitive scaffold before they encounter 7 simulators. It works as a "table of contents for the cascade", so each simulator makes sense in sequence rather than appearing as isolated tools. It is pure Markdown (no new imports) and takes zero rendering cost.
- **Part 4-B:** The current "Figura 18" caption cross-references "Figura 11 arriba" but Fig. 11 is in Chapter 2, not Chapter 3. This is a broken reference that will confuse readers. The fix replaces it with a proper cross-chapter link.
- **Part 4-C:** `<UFLSVisualizer />` explains the UFLS paradox — a mechanism that is also explained in prose by the surrounding `<ForensicReveal />`. Collapsing it reduces the chapter from 7 to 6 visible heavy components on initial render, while preserving full access. The `#phaseplane` anchor in the gallery link is already used in the existing text ("simular Retrato de Fases del UFLS") confirming the gallery has this anchor.

### Verification for Part 4
- The chronology table appears immediately before `## Fase 0`.
- The table has 11 rows matching the timestamps documented in the chapter text.
- The broken Fig. 18 caption is fixed with a working cross-chapter link.
- `<UFLSVisualizer />` is inside a collapsed `<details>` element.
- All other components (`PVCurveSimulator`, `ANSI59Cascade`, `FrequencyChart`, `TapLagSequence`, `StickyCollapse`, `BlackoutPropagationMap`, `ForensicReveal` ×3, `CuestionAbierta` ×4+) are **completely untouched**.
- All imports at the top of the file are unchanged.

---

## IMPROVEMENT 5 — `01-introduccion.mdx`: Add a brief Portugal mention in the vulnerability section

### File to edit
`tfg-antigravity-docs/docs/01-introduccion.mdx`

### What to change
Find the paragraph that contains this exact sentence (in the "La vulnerabilidad estructural del sistema ibérico" section):

```
A esta fragilidad interna se añade el aislamiento estructural de la península ibérica.
```

The paragraph ends with:

```
...la vulnerabilidad que hizo imposible frenar la cascada de sobretensiones a las 12:33:29.741 CEST.
```

**After** this paragraph and **before** the `<ForensicTable title="TABLA 1 | GRID INTERCONNECTION SNAPSHOT"` block, insert the following:

```mdx
:::note Alcance geográfico
El colapso afectó simultáneamente a España y Portugal. El sistema de REN (Redes Energéticas Nacionais) estaba eléctricamente sincronizado con REE a través de las interconexiones de alta tensión; al producirse el cero de tensión en el sistema peninsular español, la red portuguesa colapsó en milisegundos. La potencia demandada sin suministro en Portugal alcanzó los ~5,8 GW, y la reposición completa del sistema portugués requirió ~12 horas (frente a las ~15,5 horas del sistema español). Los capítulos 4 y 7c analizan la dimensión portuguesa con detalle. A lo largo de este trabajo, "sistema ibérico" designa el área síncrona conjunta ES+PT, y "sistema peninsular" se usa exclusivamente cuando el análisis se restringe a la red española.
:::
```

### Why
The Introduction frames the event as affecting "57 million people" (a figure that includes Portugal) but never explicitly establishes Portugal as a co-affected system at the technical level. Many readers will assume from the title and framing that this is a Spain-only analysis. This :::note callout: (a) establishes the iberian scope from the first chapter; (b) defines the terminological convention "sistema ibérico" vs "sistema peninsular" which is used inconsistently across chapters; (c) gives the Portuguese restoration timeline for comparison; (d) requires no new imports.

### Verification
- The `:::note` block appears between the vulnerability paragraph and `<ForensicTable title="TABLA 1 | GRID INTERCONNECTION SNAPSHOT"`.
- The existing `<ForensicTable>` content is completely untouched.
- No other text in the file was modified.

---

## EXECUTION ORDER

Execute the improvements in this exact order to minimize the risk of conflicting edits:

1. `intro.mdx` — Improvement 1 (append metadata div)
2. `01-introduccion.mdx` — Improvement 2 (add :::info key figures table)
3. `01-introduccion.mdx` — Improvement 5 (add :::note Portugal scope)
4. `02-contexto.mdx` — Improvement 3 (collapse SwingEquationSimulator)
5. `03-analisis-incidente.mdx` — Improvement 4-A (add chronology table)
6. `03-analisis-incidente.mdx` — Improvement 4-B (fix Figure 18 reference)
7. `03-analisis-incidente.mdx` — Improvement 4-C (collapse UFLSVisualizer)

Steps 2 and 5 both edit `01-introduccion.mdx` — read the file fresh before each edit, and after step 2 save and re-read before applying step 5.

---

## EXPECTED OUTPUT FORMAT

For each file edit, output:

```
### EDIT: <filename>
**Change:** <one-line description>
**Lines affected:** <approximate line range or "appended">
**Status:** ✅ Done / ❌ Failed (reason)
**Diff summary:**
+ <added lines>
- <removed lines>
```

If any edit fails or a component reference is ambiguous, stop and report the issue before proceeding — do not guess or approximate.

---

## WHAT NOT TO DO

- ❌ Do not rewrite or paraphrase any existing prose.
- ❌ Do not add new `import` statements for components that do not yet exist.
- ❌ Do not change any `sidebar_position`, `id`, or other frontmatter values.
- ❌ Do not modify any `<ForensicReveal>` props or content.
- ❌ Do not modify any existing `<ForensicTable>` content.
- ❌ Do not rename any existing `{#anchor}` heading IDs.
- ❌ Do not remove the `<BrowserOnly>` wrapper in `03-analisis-incidente.mdx` if present.
- ❌ Do not alter `galeria-graficas.mdx` — the gallery anchors (`#swing`, `#phaseplane`) are referenced but the file itself is read-only for this batch.
- ❌ Do not touch any file outside `tfg-antigravity-docs/docs/` in this batch.

---

## CONTEXT FOR THE AI AGENT

This TFG (Trabajo de Fin de Grado) is a Docusaurus v3 site with MDX files. Key technical facts you must not contradict in any new prose:

- The blackout mechanism was **overvoltage-driven** (inestabilidad capacitiva en el plano Q-V), NOT a frequency/inertia collapse.
- System inertia at collapse: **H = 2.3 s** (peninsular average), **1.3 s** (southern zone), **1.84 s** (central zone). The 2.3 s figure was above ENTSO-E's 2.0 s threshold — inertia was NOT the cause.
- IBR penetration at 12:30 CEST: **82%** of instantaneous mix (53% solar PV + ~11% wind).
- Root-cause trigger: transformer 400/220 kV in Granada tripped at **12:32:56.993 CEST**, losing 355 MW active + 165 MVAr reactive absorption.
- Total cascade duration: **32 seconds** (12:32:57 → 12:33:29.741 CEST).
- Capacitive injection from meshing: **1,050 MVAr** (NREL estimate) / **2.4 GVAr** (IIT-ICAI estimate) — both figures must remain as a disputed range, never resolved to one value.
- Portugal demand lost: **~5.8 GW**. Spain demand lost: **25.2 GW**.
- Restoration: Portugal **~12 h**, Spain **~15.5 h**, full iberian system **18.5 h** (07:05 April 29).

Any new text you write must be consistent with these facts.
